# Late fees

A world state written once — but the file it lives in names it twice, so the
link below could mean either section. That is an error, not a guess.

[A stocked library](./shared.md#a-stocked-library)

I borrow a book.
