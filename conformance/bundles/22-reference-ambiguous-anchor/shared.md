# Shared world states

Two headings with the same text slug identically (`#a-stocked-library` on
both; GitHub would call the second `#a-stocked-library-1`), so a reference to
that anchor is ambiguous.

## A stocked library

I shelve 3 books.

## A stocked library

I shelve 5 books.
