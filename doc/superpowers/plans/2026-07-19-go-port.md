# Go port — task plan

Date: 2026-07-19
Status: in progress

Implements the Go port per [ADR 0010](../../adr/0010-go-port.md),
[ADR 0011](../../adr/0011-go-test-integration.md), and the two design specs
([core](../specs/2026-07-19-go-core-port-design.md),
[runner+adapter](../specs/2026-07-19-go-runner-adapter-design.md)). Each core
stage is gated by reproducing the shared goldens byte-for-byte before the next.

## Runtime (done)

- [x] Scaffold `go/` module + Go toolchain (asdf `golang 1.26.3`).
- [x] Stage 1 — parse → `var-doc.json`: offsets (UTF-16 layer), span, value,
      canonical JSON, ast, table_cells, scanner, structurer, parse. 15/15.
- [x] Stage 2 — `registry.json`: expression (cucumber-expressions-go wrapper),
      registry, handler, step_kind/role; author facade (`Steps` builder);
      15 `*.steps.go` fixtures symlinked into `go/conformance/bNN`. 15/15.
- [x] Stage 3 — `plan.json`: sentences, matcher, diagnostics, RowCheck, plan. 15/15.
- [x] Stage 4 — `trace.json`: failure model, doc-string/param/cell diffs,
      failure anchor, executor, trace projection + RunConformance. 15/15.
- [x] Drift (unit-gated): hash (FNV-1a/UTF-16), drift (Jaccard 0.5),
      BaselineStore, lockfile serializer/parser; translated hash/drift tests.
- [x] `varconfig`: strict reader; 8/8 config cases.
- [x] `varrunner`: discovery, run, render, filesystem baseline store; unit tests.
- [x] `vargotest`: `go test` subtest-per-example adapter + drift gate; unit tests.

## Repo & release integration

- [ ] Root `Makefile` `go` target (build + test + vet + example projects),
      threaded into `check:`; header comment updated.
- [ ] `make coverage` step emitting `go/coverage/lcov.info`
      (`go test -coverprofile` → lcov); README coverage table row + `N ports`
      prose (via `scripts/coverage-summary.sh`).
- [ ] `.github/workflows/go.yml` (triggered on `go/**`, `conformance/**`,
      `examples/**`, the workflow file).
- [ ] `examples/go-gotest/` standalone consumer project + `examples/README.md` row.
- [ ] `languages.json` entry (`go`, ext `.steps.go`, commands) + `SiteLang` union.
- [ ] `release/lint-commits.sh` consumer-scope `go`, `cliff.toml` section map,
      `release/targets/NN-go-modules.sh` (Go module tag publishing — may be parked).

## Follow-ups (deliberately not in the initial port)

- [ ] **Reflection-based typed handlers**, godog-style: `func(name string, age
      int) error` resolved by runtime reflection, as an *additive* second form
      alongside the explicit `func(state Value, args []Value) (*Value, error)`.
      Needs a reflect-based `Value` coercion layer plus the trailing
      table/doc-string slot case, and a second Go consumer to validate against.
      See the handler-shape fork point in [ADR 0010](../../adr/0010-go-port.md).

## Website / LSP integration

- [ ] Tree-sitter dialect `tree-sitter-dialects/go.ts` + scanner/loaders/bundler
      wiring; `extraction-conformance` + `tree-sitter-scanner-go` tests.
- [ ] Website docs `<Tabs syncKey="lang">` Go tabs; front-page `<Editor>` `<File>`
      tabs; `cm-languages.ts` Go highlighter (`language-coverage.test.ts` gate).
