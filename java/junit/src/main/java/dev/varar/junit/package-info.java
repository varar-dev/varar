/**
 * The var JUnit Platform TestEngine (id "varar"): one container per discovered
 * oath file, one leaf per example. Delegates all pipeline/orchestration logic to
 * varar-runner/varar-core; contains only JUnit-Platform glue (discovery selectors,
 * descriptor tree, execution, config bridging).
 */
package dev.varar.junit;
