/**
 * The var JUnit Platform TestEngine (id "var"): one container per discovered
 * spec file, one leaf per example. Delegates all pipeline/orchestration logic to
 * var-runner/var-core; contains only JUnit-Platform glue (discovery selectors,
 * descriptor tree, execution, config bridging).
 */
package com.oselvar.var.junit;
