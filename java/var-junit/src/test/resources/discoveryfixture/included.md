# Included

A fixture spec resource used by `DiscoverySelectorResolverTest` to prove discovery includes a
`.md` resource matching `var.vars.include` and not matching `var.vars.exclude`. Its content is
irrelevant to this task — discovery (Task 9) creates a container without parsing it.
