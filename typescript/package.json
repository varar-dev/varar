{
  "name": "oselvar-var",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r --filter '!@oselvar/website' --filter '!@oselvar/website' build",
    "typecheck": "tsc -p tsconfig.tests.json",
    "test": "NODE_OPTIONS=\"--import tsx\" vitest run",
    "test:coverage": "NODE_OPTIONS=\"--import tsx\" vitest run --coverage",
    "test:watch": "NODE_OPTIONS=\"--import tsx\" vitest",
    "lint": "biome check . && node scripts/lint-no-reexports.mjs",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "knip": "knip",
    "jscpd": "jscpd packages",
    "install:vscode": "pnpm -r --filter @oselvar/var-lsp --filter oselvar-var build && node scripts/install-vscode.mjs",
    "check": "pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm knip && pnpm jscpd"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/node": "^26.1.0",
    "@vitest/coverage-v8": "^4.1.9",
    "jscpd": "^5.0.11",
    "knip": "^6.24.0",
    "tsx": "^4.22.5",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
