# @varar/lsp

The Language Server Protocol server for Varar — diagnostics, semantic tokens, and rename
support for `.md` specs and their step definitions. Consumed by editor extensions
such as `@varar/varar-vscode`.
