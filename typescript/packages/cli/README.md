# @varar/cli

The `varar` command line for Varar oaths: `varar init` and `varar lint`.
Oaths themselves run through your project's own test runner — see
[@varar/vitest](https://www.npmjs.com/package/@varar/vitest).
The imperative shell around `@varar/core`.
