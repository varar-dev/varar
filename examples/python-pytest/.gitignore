.venv/
__pycache__/
.pytest_cache/
# Artifact of `uv run` here: the lock pins path/git sources that differ
# between the oselvar/varar monorepo and the var-examples repo. In your own
# project, DO commit your lockfile.
uv.lock
