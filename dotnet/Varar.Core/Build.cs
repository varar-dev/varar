namespace Varar.Core;

/// <summary>
/// Placeholder anchor so the freshly-scaffolded <c>Varar.Core</c> assembly has a
/// public symbol to smoke-test against. Removed once real pipeline modules land
/// (T1: Value / CanonicalJson / Span).
/// </summary>
public static class Build
{
    public const string Marker = "varar-core";
}
