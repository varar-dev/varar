[project]
name = "varar-config"
version = "0.4.2"
description = "Reads var.config.json — the shared config file for all var tools"
license = "MIT"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/varar_config"]
