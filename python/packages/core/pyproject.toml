[project]
name = "varar-core"
version = "0.8.0"
description = "Markdown-native BDD — pure functional core engine"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["cucumber-expressions==20.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/varar_core"]
