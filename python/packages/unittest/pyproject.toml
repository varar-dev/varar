[project]
name = "varar-unittest"
version = "0.6.0"
description = "unittest adapter for Markdown-native BDD"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["varar==0.6.0", "varar-config==0.6.0", "varar-core==0.6.0", "varar-runner==0.6.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/varar_unittest"]
